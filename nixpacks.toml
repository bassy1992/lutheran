[phases.build]
cmds = ["cd backend && python manage.py collectstatic --noinput"]

[start]
cmd = "cd backend && python manage.py migrate && gunicorn church_backend.wsgi:application --bind 0.0.0.0:$PORT"
